<View>
  <Style>
    .ls-group { 
      margin-bottom: 20px; 
      padding: 15px; 
      border: 1px solid #e0e0e0; 
      border-radius: 8px; 
    }
    .ls-header { 
      font-size: 16px; 
      margin-bottom: 5px;
      border-bottom: 1px solid #ddd; 
      padding-bottom: 5px;
    }
  </Style>
  
  <!-- Text to annotate -->
  <View style="padding: 10px; font-size: 16px; line-height: 1.6; border-bottom: 2px solid #ccc; margin-bottom: 20px;">
    <Text name="text" label="note_content" value="$note_content" />
  </View>

  <!-- STEP 1: Highlight person mentions (to link needs to) -->
  <View className="ls-group">
    <View className="ls-header">
        <Text name="hdr_entities" value="Step 1: Highlight All Person Mentions &amp; Missed PII"/>
        <Text className="instruction" name="instruction_1" value="Highlight any reference to a person or personally identifiable information (PII)." />
      </View>
    <Labels name="entity_labels" toName="text" choice="multiple">
      <Label value="Pseudonym" background="#8e44ad" hint="Autogenerated label - real name was replaced with a pseudonym." />
      <Label value="Pronoun" background="#95a5a6" hint="he, she, they, him, her, them, his, her, their"/>
      <Label value="Descriptive Role" background="#7f8c8d" hint="the tenant, the carer, mother, father, partner, resident"/>
      <Label value="Missed Person Name" background="#000000" hint="Real name Presidio missed" />
      <Label value="Missed Other PII" background="#333333" hint="Other PII Presidio missed (email address, phone number, postcode, any discernable system IDs)" />
    </Labels>
  </View>

  <!-- STEP 2: Highlight need spans and classify them -->
  <View className="ls-group">
    <View className="ls-header">
      <Text name="hdr_classification" value="Step 2: Highlight &amp; Classify Additional Needs"/>
      <Text className="instruction" name="instruction_2" value="Select a label below, then highlight the exact text mentioning the need" />
    </View>
    <Filter name="filter_needs" toName="need_labels" placeholder="Search additional needs..." hotkey="shift+h" />
    <Labels name="need_labels" toName="text" choice="multiple">
      <AN_PLACEHOLDER>
    </Labels>
  </View>

  <!-- STEP 3: Link needs to person mentions -->
  <View className="ls-group">
    <View className="ls-header">
      <Text name="hdr_relations" value="Step 3: Link Each Need to the Nearest Person Mention"/>
      <Text className="instruction" name="instruction_3" value="Click a highlighted need span, then click a highlighted person mention to create a link" />
    </View>
    <Relations>
      <RELATION_PLACEHOLDER>
    </Relations>
  </View>
</View>